defaults:
  - visualize_cross_session_depth
  - _self_

# Override target extrinsics to use direct ICP results
target:
  extrinsics_dir: output/icp_comparison_${reference.session}_${target.session}
  extrinsics_filename: recovered_extrinsics_direct_icp.json

output:
  dir: output/cross_session_alignment_direct_icp